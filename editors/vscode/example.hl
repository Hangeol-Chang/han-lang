// 한랭(han-lang) 예제 — 확장 동작 확인용

사과는 3 이다.
인삿말은 "안녕하세요!" 이다.
(인삿말)를 출력한다.

함수 더하기(a, b)
\t(a + b)를 반환한다.

합은 더하기(3, 4) 이다.
만약 (합 < 10) 라면
\t("작다")를 출력한다.
아니면
\t("크다")를 출력한다.

숫자들은 [1, 2, 3] 이다.
(길이(숫자들))를 출력한다.

i는 1 이다.
(i <= 5) 동안
\t(i)를 출력한다.
\ti는 i + 1 이다.
