// 한랭 함수 예제 — 정의, 반환, 재귀, 호이스팅

함수 더하기(a, b)
	(a + b)를 반환한다.

함수 팩토리얼(n)
	만약 (n <= 1) 라면
		(1)를 반환한다.
	(n * 팩토리얼(n - 1))를 반환한다.

합은 더하기(3, 4) 이다.
(합)를 출력한다.

(팩토리얼(6))를 출력한다.

// 정의보다 먼저 호출해도 동작 (호이스팅)
인사하기("영희")

함수 인사하기(이름)
	("%s님, 환영합니다!\n", 이름)를 출력한다.
