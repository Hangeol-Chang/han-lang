// 한랭 기초 예제 — 변수, 자료형, 출력

이름은 "철수" 이다.
나이는 17 이다.
키는 172.5 이다.
합격은 진실 이다.

("이름: %s\n", 이름)를 출력한다.
("나이: %d세\n", 나이)를 출력한다.
("키: %s cm\n", 키)를 출력한다.

만약 (합격 == 진실) 라면
	("합격을 축하합니다!")를 출력한다.
아니면
	("다음 기회에 도전하세요.")를 출력한다.

소개는 이름 + "님은 " + 나이 + "살입니다." 이다.
(소개)를 출력한다.
